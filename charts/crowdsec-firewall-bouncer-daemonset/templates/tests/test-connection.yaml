apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crowdsec-firewall-bouncer-daemonset.fullname" . }}-test-connection"
  labels:
    {{- include "crowdsec-firewall-bouncer-daemonset.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crowdsec-firewall-bouncer-daemonset.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
